{
  "name": "Python Dev container",
  "build": {
    "dockerfile": "Dockerfile.dev",
    "context": ".."
  },

  "runArgs": [
    "--env-file",
    "${localWorkspaceFolder}/.env.dev"
  ],
  "mounts": [
    "source=${localEnv:HOME}/.config/nvim,target=/home/vscode/.config/nvim,type=bind"
  ],
  "forwardPorts": [8000],
  "remoteUser": "vscode",
  "postCreateCommand": "[ ! -d ~/.config/nvim ] && git clone https://github.com/LazyVim/starter ~/.config/nvim || true; uv sync -q"
}
